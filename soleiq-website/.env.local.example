# Public Supabase credentials (safe in browser code; RLS enforces access).
# Use the SAME values as soleiq-web/.env.local — single shared backend.

VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY
