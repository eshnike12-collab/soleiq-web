# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Project artifacts (weights, indices, eval reports — never commit)
artifacts/

# Synthetic sample data (regenerated by `make sample-data`)
data/sample/

# Real patient images and labels — NEVER commit. PHI risk.
data/raw/
data/reference_bank/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Env / secrets
.env
.env.local

# Local debugging data: scan videos, extracted frames, SQLite metadata.
# Patient media — never commit.
local-data/
